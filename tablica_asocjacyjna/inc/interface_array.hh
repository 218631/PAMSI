#ifndef INTTABLICA_HH
#define INTTABLICA_HH
#include <cstdlib>


class interface_array{
public:
  virtual void add(int x)=0;
  virtual void power(int x)=0;
  virtual void multiply(int x)=0;
};

#endif
