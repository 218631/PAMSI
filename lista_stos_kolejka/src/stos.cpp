#include "stos.hh"
/*
stos::stos(int rozmair){
  pojemnosc=rozmiar;
  tablica_stosu = new int[pojemnosc];
  gora = -1;
}

stos::~stos(){
  delete []tablica_stosu;
}

void stos::dodaj_element(int element){
  if(tablica_stosu[gora]==Null)
    {
      tablica_stosu[gora]=element;
    }
  else
    {
      tablica_stosu2 = new int[pojemnosc+1] 
	tablica_stosu2[gora] = element; 
      for(int i=0; i < pojemnosc+1;i++)
	{
	   tablica_stosu2[i+1]=tablica_stosu[i];
	}
      delete []tablica_stosu;
      pojemnosc++;
      tablica_stosu = new int[pojemnosc]
      tablica_stosu = tablica_stosu2;
    }
}
int stos::usuń_element(){
  int kieszen;
  kieszen=tablica_stosu[gora];
  tablica_stosu[gora]=Null;
  return kieszen;
}

bool stos::pusty(){
  (return (gora<0));
}
*/
